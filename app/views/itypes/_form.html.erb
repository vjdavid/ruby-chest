<%= form_for(@itype) do |f| %>
  <% if @itype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@itype.errors.count, "error") %> prohibited this itype from being saved:</h2>

      <ul>
      <% @itype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
